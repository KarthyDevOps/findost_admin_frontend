export const privilegesData = (payload) => {
  return {
    payload,
    type: "ADMIN_PRIVILEGES",
  };
}

export const staffDetails = (payload) => {
  return {
    payload,
    type: "STAFF_DETAILS",
  };
}

